<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_settings"
    android:layout_width="fill_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"

    tools:context="com.example.chandler.assassinsapp.Settings">

    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content">
        <Button android:id="@+id/logoutButton"
                android:layout_width="300dp"
                android:layout_height="50dp"
                android:text="Logout" />
    </LinearLayout>

    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content">
        <ToggleButton android:id="@+id/notificationsButton"
            android:layout_width="300dp"
            android:layout_height="50dp"
            android:textOff="Notifications Disabled"
            android:textOn="Notifications Enabled"
            android:text="Leave Current Game"
            />
    </LinearLayout>

    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content">
        <Button android:id="@+id/leaveGameButton"
            android:layout_width="300dp"
            android:layout_height="50dp"
            android:text="Leave Current Game"
            />
    </LinearLayout>

</LinearLayout>
